import { SolanthueFilterPipe } from './solanthue-filter.pipe';

describe('SolanthueFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new SolanthueFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
