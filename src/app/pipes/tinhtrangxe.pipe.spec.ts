import { TinhtrangxePipe } from './tinhtrangxe.pipe';

describe('TinhtrangxePipe', () => {
  it('create an instance', () => {
    const pipe = new TinhtrangxePipe();
    expect(pipe).toBeTruthy();
  });
});
